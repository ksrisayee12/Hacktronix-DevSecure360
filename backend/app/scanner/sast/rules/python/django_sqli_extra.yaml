rule_id: python_django_sqli_extra_001
vuln_class: SQLi
severity: Critical
issue: Django ORM .extra() SQL Injection (CWE-89)
cwe: CWE-89
sources:
- request.GET
- request.POST
- request.data
sinks:
- .extra(
- .raw(
- cursor.execute(
sanitizers:
- params=
- '%s'
remediation: Use Django ORM filters. Avoid .extra() and .raw() with user input.
