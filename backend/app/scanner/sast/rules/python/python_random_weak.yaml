# RESEARCH EVIDENCE
# CWE Source:      https://cwe.mitre.org/data/definitions/330.html
# CodeQL Source:   https://codeql.github.com/codeql-standard-libraries/
# Semgrep Source:  https://semgrep.dev/r?q=insecure+randomness
# OWASP Cheat:     https://cheatsheetseries.owasp.org/cheatsheets/Cryptographic_Storage_Cheat_Sheet.html
# Verification:    Non-cryptographic PRNGs used for security tokens are predictable.

rule_id: python_random_weak
language: python
vuln_class: Weak Crypto
severity: Medium
cwe: CWE-330
owasp: A02:2021-Cryptographic Failures
cvss_score: 5.3
cvss_vector: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N
confidence: Tentative
issue: Insecure Randomness via random module
message: |-
  The random module in Python (random.randint, random.choice) is predictable and should not be used for passwords, CSRF tokens, or cryptographic keys.
sources: []
sinks:
- random.randint(
- random.choice(
sanitizers:
- secrets.token_hex(
- os.urandom(
remediation: |-
  Use the secrets module for cryptographically strong randomness.

  SAFE:
    import secrets
    token = secrets.token_hex(32) Always review your code and apply strict bounds or input limitations. Use static analysis tools to verify fixes.
