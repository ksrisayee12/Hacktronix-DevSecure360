rule_id: python_host_header_001
vuln_class: Host Header Injection
severity: Medium
issue: Host Header Injection (CWE-644)
cwe: CWE-644
sources:
- request.host
- request.headers.get('Host'
sinks:
- redirect(
- url_for(
sanitizers: []
remediation: Validate Host header against a whitelist. Use absolute URLs from configuration.
