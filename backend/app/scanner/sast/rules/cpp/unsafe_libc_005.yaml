rule_id: c_unsafe_libc_005
vuln_class: Memory Corruption
severity: Critical
issue: 'Unsafe C function: vsprintf( vsprintf() without bounds — use vsnprintf() (CWE-120)'
cwe: CWE-120
sources: []
sinks:
- vsprintf(
sanitizers:
- snprintf(
- strlcpy(
- strlcat(
- fgets(
remediation: 'Replace vsprintf( with a bounds-checked alternative: snprintf/strncat/fgets/strtok_r.'
