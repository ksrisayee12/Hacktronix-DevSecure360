rule_id: c_unsafe_libc_002
vuln_class: Memory Corruption
severity: Critical
issue: 'Unsafe C function: strcat( strcat() is unsafe — no bounds checking (CWE-120)'
cwe: CWE-120
sources: []
sinks:
- strcat(
sanitizers:
- snprintf(
- strlcpy(
- strlcat(
- fgets(
remediation: 'Replace strcat( with a bounds-checked alternative: snprintf/strncat/fgets/strtok_r.'
