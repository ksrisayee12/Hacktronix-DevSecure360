rule_id: php_laravel_003
vuln_class: SQLi
severity: Critical
issue: 'Laravel: Eloquent whereRaw injection (CWE-89)'
cwe: CWE-89
sources:
- $_GET
- $_POST
- $_REQUEST
- $_COOKIE
- $_SERVER
- $_FILES
- getallheaders
- file_get_contents('php://input')
sinks:
- ->whereRaw(
sanitizers:
- DB::select(
- '?'
- bindings
remediation: Use parameterized bindings with ->whereRaw(. Never interpolate user data
  into raw queries.
