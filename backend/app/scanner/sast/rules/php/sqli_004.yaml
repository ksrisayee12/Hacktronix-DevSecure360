rule_id: php_sqli_004
vuln_class: SQLi
severity: Critical
issue: SQL Injection via OO mysqli (CWE-89)
cwe: CWE-89
sources:
- $_GET
- $_POST
- $_REQUEST
- $_COOKIE
- $_SERVER
- $_FILES
- getallheaders
- file_get_contents('php://input')
sinks:
- $mysqli->query(
sanitizers:
- prepare(
- bindParam(
- bindValue(
- real_escape_string
remediation: Use prepared statements with OO mysqli. Never concatenate user data into
  SQL.
