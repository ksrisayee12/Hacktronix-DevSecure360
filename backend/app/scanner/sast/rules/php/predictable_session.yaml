rule_id: php_predict_session_001
vuln_class: Insecure Randomness
severity: Medium
issue: Predictable session via uniqid() or rand() (CWE-330)
cwe: CWE-330
sources: []
sinks:
- uniqid(
- rand(
- mt_rand(
sanitizers:
- random_bytes(
- random_int(
remediation: Use random_bytes() or openssl_random_pseudo_bytes() for session tokens.
