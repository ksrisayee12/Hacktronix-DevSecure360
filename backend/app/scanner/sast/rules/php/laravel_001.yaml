rule_id: php_laravel_001
vuln_class: SQLi
severity: Critical
issue: 'Laravel: Laravel raw SQL (CWE-89)'
cwe: CWE-89
sources:
- $_GET
- $_POST
- $_REQUEST
- $_COOKIE
- $_SERVER
- $_FILES
- getallheaders
- file_get_contents('php://input')
sinks:
- DB::statement(
sanitizers:
- DB::select(
- '?'
- bindings
remediation: Use parameterized bindings with DB::statement(. Never interpolate user
  data into raw queries.
