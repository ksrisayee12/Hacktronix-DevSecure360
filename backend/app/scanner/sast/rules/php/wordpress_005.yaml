rule_id: php_wp_005
vuln_class: XSS
severity: High
issue: 'WordPress: WordPress direct POST output (CWE-89 / CWE-79)'
cwe: CWE-89
sources:
- $_GET
- $_POST
- $_REQUEST
- $_COOKIE
- $_SERVER
- $_FILES
- getallheaders
- file_get_contents('php://input')
sinks:
- echo $_POST[
sanitizers:
- esc_html(
- esc_attr(
- sanitize_text_field(
- wpdb->prepare(
remediation: 'Use echo $_POST[ with proper sanitization: wpdb->prepare() for SQL,
  esc_html() for output.'
