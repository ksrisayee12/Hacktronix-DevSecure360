rule_id: c_unsafe_libc_001
vuln_class: Memory Corruption
severity: Critical
issue: 'Unsafe C function: strcpy( strcpy() is unsafe — no bounds checking (CWE-120)'
cwe: CWE-120
sources: []
sinks:
- strcpy(
sanitizers:
- snprintf(
- strlcpy(
- strlcat(
- fgets(
remediation: 'Replace strcpy( with a bounds-checked alternative: snprintf/strncat/fgets/strtok_r.'
