# RESEARCH EVIDENCE
# CWE Source:      https://cwe.mitre.org/data/definitions/22.html
# CodeQL Source:   https://codeql.github.com/codeql-standard-libraries/
# Semgrep Source:  https://semgrep.dev/r?q=path+traversal
# OWASP Cheat:     https://cheatsheetseries.owasp.org/cheatsheets/File_Upload_Cheat_Sheet.html
# Verification:    User input in file paths without path sanitization allows arbitrary file read/write.

rule_id: c_path_traversal_fopen
language: c
vuln_class: Path Traversal
severity: High
cwe: CWE-22
owasp: A01:2021-Broken Access Control
cvss_score: 7.5
cvss_vector: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N
confidence: Confirmed
issue: Path Traversal via fopen() with unvalidated input
message: |-
  Using unvalidated user input to construct a file path for fopen() allows an attacker to read or write arbitrary files on the system using directory traversal characters (../).
sources: []
sinks:
- fopen(
sanitizers:
- realpath(
- basename(
remediation: |-
  Sanitize user input by allowing only alphanumeric characters, or strictly validate against an allowlist of acceptable file names. Avoid direct file access based on user input. Always review your code and apply strict bounds or input limitations. Use static analysis tools to verify fixes.
